---
"ghostty-config-editor": patch
---

Replace the app icon with Ghostty's official blueprint logo. Adds `icons`, `icons:fetch`, and `icons:generate` npm scripts to keep the icon in sync with upstream.
